<UserControl x:Class="TechnoSphere_2025.controls.header.HeaderControl_Admin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TechnoSphere_2025.controls.header"
             xmlns:controlsHeaderBase="clr-namespace:TechnoSphere_2025.controls.header"
             Width="1000" Height="80">
    <Grid Style="{DynamicResource ForegroundColor}">
        <Grid Grid.Row="0" Style="{DynamicResource ForegroundColor}">
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="45"/>
            </Grid.RowDefinitions>

            <controlsHeaderBase:HeaderControlBase x:Name="HeaderControlBase"/>

            <!-- Header_2 -->
            <Grid Grid.Row="1" Margin="5,0,15,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="45"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        x:Name="BurgerCatalogButton"
                        Style="{DynamicResource ButtonIconColor}"
                        Click="BurgerCatalogButton_Click">
                    <Image x:Name="BurgerCatalogImage"
                           Source="{DynamicResource BurgerIcon}"
                           Width="25"/>
                </Button>
                <Button Grid.Column="1"
                    Style="{DynamicResource ButtonIconColor}"
                    Margin="5,0,0,0">
                    <TextBlock Text="{DynamicResource Loc_Stock}"
                           Style="{StaticResource Headline_3}"
                           Margin="10,0,10,0"/>
                </Button>

                <Grid Grid.Column="2" Margin="15,5,15,5">
                    <TextBox x:Name="SearchTextBox"
                         Style="{DynamicResource SearchTextboxColor}"
                         Height="35"
                         Padding="35,0,5,0"/>
                    <Button Style="{DynamicResource SearchButtonIconColor}"
                        Width="35"
                        HorizontalAlignment="Left">
                        <Image Source="{DynamicResource SearchIcon}" Height="25"/>
                    </Button>
                </Grid>

                <Button Grid.Column="3"
                        Style="{DynamicResource ButtonIconColor}">
                    <TextBlock Text="{DynamicResource Loc_Orders}"
                        Style="{StaticResource Headline_3}"
                        Margin="10,0,10,0"/>
                </Button>
            </Grid>
        </Grid>

        <Popup x:Name="CatalogPopup"
               PlacementTarget="{Binding ElementName=BurgerCatalogButton}"
               Placement="Bottom"
               AllowsTransparency="True"
               Opened="CatalogPopup_Opened"
               Closed="CatalogPopup_Closed">
            <Border Style="{DynamicResource CatalogCategoryPopupColor}"
                    MinHeight="90"
                    Width="980"
                    Margin="0, 0, 0, 10"
                    Padding="10, 30, 0, 30">
                <ScrollViewer  VerticalScrollBarVisibility="Hidden">
                    <ItemsControl ItemsSource="{Binding CatalogVm.Categories}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="4" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <!-- Категории -->
                                    <TextBlock Text="{Binding DisplayName}"
                                       Style="{DynamicResource HeadlineColor_2-1}"/>
                                    <!-- Подкатегории -->
                                    <ItemsControl ItemsSource="{Binding Children}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Margin="0,10,0,0" Style="{StaticResource Mainline_2}">
                                            <Hyperlink Style="{DynamicResource HyperLinkColor}"
                                                       Command="{Binding DataContext.NavigateCategoryCommand, 
                                                       RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                       CommandParameter="{Binding CategoryID}"
                                                       TextDecorations="{x:Null}">
                                                <Run Text="{Binding DisplayName,  Mode=OneWay}"/>
                                            </Hyperlink>
                                                </TextBlock>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Popup>
    </Grid>
</UserControl>
